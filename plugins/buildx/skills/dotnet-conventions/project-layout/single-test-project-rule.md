# The single test project rule (moved)

The single-`[Company].[Product].Test` rule, surface-folder layout, per-class `DistributedApplication` mount, and method naming `{Action}_{Scenario}_{Expectation}` all live in the **`dotnet-testing`** skill (§ layout, § mstest-integration).

Load `dotnet-testing` and read `layout.md` and `mstest-integration.md`.
